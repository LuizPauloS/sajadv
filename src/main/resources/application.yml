server:
  servlet:
    context-path: /sajadv-service/v1

spring:
  profiles:
    active: default
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.MySQL5Dialect
        show_sql: false
        use_sql_comments: true
        format_sql: true
        type: trace

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:  jdbc:mysql://localhost:3306/sajadv?useTimezone=true&serverTimezone=UTC
    username: root
    password: ep25dbbr